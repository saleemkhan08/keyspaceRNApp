export interface Service {
  description: string;
  id: string;
  name: string;
  order: string;
  photoUrl: string;
}

export interface ProductCategory {
  id: string;
  name: string;
  photoUrl: string;
}
